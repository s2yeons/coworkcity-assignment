# 개발 기록

프로젝트를 진행하면서 떠오른 개선사항, 결정 사항, 해야 할 일을 자유롭게 기록하는 파일입니다.

## 개선사항 / TODO

- [x] 백엔드에 DB 연결 (Prisma 7 + PostgreSQL, 2026-09-11)
- [x] 백엔드 에러 핸들링 미들웨어 추가 (`{ error: { code, message } }` 규약)
- [x] 백엔드 요청 유효성 검사 (zod)
- [ ] 백엔드 ESLint 설정 추가
- [ ] 프론트 공통 레이아웃 / 컴포넌트 구조 잡기
- [x] 추천 기능 mock data를 백엔드 API + DB로 이전
- [ ] 업종 검색을 pg_trgm 인덱스 기반으로 개선 (현재 ILIKE full scan, 데이터 규모 커지면 필요)
- [ ] 추천 API 응답 캐싱 (조건 조합이 적어 캐시 효율 높음)
- [ ] 배포: Postgres(Neon 등) 연결 + `prisma migrate deploy`
- [ ] 다크 모드 대응 (현재 추천 페이지는 라이트 고정)
- [ ] 업종 mock 30개 → 코워크시티 전체 업종으로 확장
- [ ] 배포 환경변수 정리 (`CLIENT_URL`, `NEXT_PUBLIC_API_URL`)

## 결정 사항

| 날짜 | 내용 | 이유 |
|---|---|---|
| 2026-09-10 | 프론트 `frontend/`, 백엔드 `backend/`로 분리 | Next.js도 서버 렌더링을 하므로 `client/server`보다 역할이 명확함 |
| 2026-09-10 | 백엔드는 Express + TypeScript | 프론트와 같은 언어라 진입장벽이 낮고 자료가 가장 많음 |
| 2026-09-10 | npm workspaces로 루트에서 통합 관리 | `npm run dev` 한 번으로 양쪽 실행 |
| 2026-09-11 | 추천 기능을 Express + Prisma 7 + PostgreSQL full-stack으로 전환 | 2차 과제 스펙. 프론트 mock 제거, DB가 단일 출처 |
| 2026-09-11 | API는 Next.js Route Handler가 아니라 기존 Express 백엔드에 구현 | 레포에 이미 backend/ 워크스페이스가 있어 기존 API 구조 우선 |
| 2026-09-11 | 지점-업종은 OfficeIndustry N:M (등록 가능 업종). seed가 "계약 불가 업종" 예외를 N:M으로 변환 | 스펙 §6·7 권장 스키마 |
| 2026-09-11 | 프론트 data fetching은 자체 useApiQuery 훅 (TanStack Query 미도입) | 스펙 §26: 기존에 없으면 라이브러리 추가 금지 |
| 2026-09-11 | 지점 데이터는 "등록 가능 업종"이 아닌 "계약 불가 업종" 예외 목록 | 실제 코워크시티 상세 페이지 구조와 동일 |
| 2026-09-11 | 선택 조건(비과밀/인허가/가격)은 필터가 아니라 우선순위 점수 | 스펙 §10. 단, 인허가 요건 업종은 인허가 지원 지점만 필수로 필터링 |
| 2026-09-11 | 필터·단계 상태를 URL search params에 유지 | 상세 페이지 갔다 와도 결과 유지, 링크 공유 가능 |

| 2026-09-11 | 사업자등록증 OCR을 기존 추천에 결합 (갈아엎지 않음) | 등록증은 추천 조건의 입력 수단. 기존 사업자(주소 이전) 사용자층으로 문제 정의 확장 |
| 2026-09-11 | OCR 엔진은 tesseract.js, 학습 데이터 레포 포함 | 채점자가 API 키·네트워크 없이 실행. 엔진은 ocr.ts 한 곳에 격리해 교체 가능 |
| 2026-09-11 | 실제 등록증 대신 가상 샘플 3장 생성 (generate.py) | 개인정보. 촬영본 변형 포함해 파서·E2E 검증 |
| 2026-09-11 | UI를 코워크시티 디자인 토큰(Pretendard, 브랜드 그린, 12px radius, 헤더/푸터)으로 재구성 | 새 기능이 사이트의 일부처럼 보이도록. 실제 사이트 computed style에서 값 확인 |

## 배운 것 / 메모

- 
